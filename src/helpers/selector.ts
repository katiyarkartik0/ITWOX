export const selectAccessToken = (state:any) => state.auth.accessToken;
export const selectUserData = (state:any) => state.auth.userData;
export const selectToastContent = (state:any) => state.toast;
